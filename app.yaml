# Runtime config for Databricks Apps (used alongside databricks.yml).
# Port/host are set by the platform via DATABRICKS_APP_PORT; the Node adapter binds 0.0.0.0 in scripts/databricks-serve.mjs.
command:
  - npm
  - run
  - databricks:start
